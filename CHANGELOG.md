# Changelog

## v1.6.0 — 2026-08-21

### 추가
- **식단 컨트롤 바가 따라온다** — 주별/월별 전환과 기간 이동이 상단바 아래에 붙고,
  아래로 읽는 동안은 물러났다가 위로 조금만 올리면 돌아온다. 예전에는 오늘 날짜로
  자동 스크롤된 뒤 토글이 화면 위 1000px 밖에 있어서 맨 위까지 되돌아가야 했다.

### 수정
- **"오늘로" 버튼이 오늘로 간다** — 이번 주를 보고 있을 때 누르면 오늘이 아니라
  맨 위(월요일)로 갔다. 오늘로부터 1207px, 그것도 반대 방향이었다. 월별에서
  오늘이 이미 펼쳐져 있을 때 아무 반응이 없던 것도 같이 고쳤다.
- **월별에서 펼친 식단이 안 잘린다** — 격자 아래쪽 날짜를 누르면 패널 아래가
  하단 메뉴에 가려서 저녁 칸이 보이지 않았다.
- **기간을 넘겨도 지난 날짜 패널이 남던 문제** — 8월 21일을 펼친 채 다음 달로
  넘기면 9월 달력 아래에 "8월 21일" 패널이 그대로 남았다. 거기서 식단을 추가하면
  화면에 없는 날짜로 등록됐다.
- **주별 → 월별 → 주별 로 돌아오면 3주 전에 가 있던 문제.**
- **한국어 조사** — "계란후라이을(를) 올렸어요" 가 "계란후라이를 올렸어요" 로.
  알림·가계부·재고·관리자 5곳. ㄹ 받침 예외도 처리한다 (서울로, 칼로).
- **냉장/냉동 아이콘이 뒤바뀌어 있었다** — 냉장 보관 식재료에 눈송이가 떠 있었다.
- **날짜 표기** — 대시보드의 "8. 21.일", 재고의 "2026-08-21", 관리자 화면의
  ISO 표기가 모두 "8월 21일" 로 통일. 한국 시간 기준으로 고정해서, 서버가 UTC 로
  준 시각이 새벽에 등록한 항목을 하루 전으로 보여주던 것도 함께 고쳤다.
- **기간을 넘겨도 "이번 주"라고 하던 빈 상태 문구** — 보고 있는 기간을 말한다.
- 기간을 빠르게 연달아 넘길 때 목록과 날짜 라벨이 서로 다른 주를 가리킬 수 있던 문제.

### 변경
- **터치 영역** — 상단바 알림·로그아웃 버튼이 40×47px 에서 48×48px 로. 모든 화면에
  있는 버튼이었다. "모두 읽음"(52×20px)과 가족 설정 스위치(48×24px)도 48px 로 넓혔다.
  월 격자 날짜 칸은 42px 에서 44.8px 로 — 7열 격자라 390px 화면에서 이게 상한이고,
  그보다 좁은 화면에서는 44px 에 못 미친다.
- **글자 대비** — 보조 텍스트 색이 흰 배경에서 4.49:1 로 WCAG AA(4.5) 를 아슬하게
  밑돌았다. 5.12:1 로 올렸다. 가계부 차트 눈금은 11px → 12px.
- **가족 화면** — "1 MEMBERS" / "MASTER" 를 "1명" / "방장" 으로.
- 브라우저 글자 크기를 크게 설정해도 컨트롤 바가 상단바에 먹히지 않는다.

## v1.5.0 — 2026-03-31

### 추가
- **이메일 저장** — 로그인 시 이메일 저장 체크박스, 다음 방문 시 자동 입력
- **자동 로그인 유지** — 브라우저/PWA를 닫았다 열어도 7일간 로그인 유지

### 보안
- CORS 와일드카드 제거, 실제 접속 origin으로 제한
- Nginx 보안 헤더 추가 (X-Content-Type-Options, X-Frame-Options, Referrer-Policy)

### 변경
- 접속 포트 80/443으로 변경 (HTTP → HTTPS 자동 리다이렉트)
- 자체 서명 인증서에 sslip.io 와일드카드 SAN 추가

### 삭제
- WebAuthn 생체인증 코드 전체 제거 (LAN 환경 비호환)

## v1.4.1 — 2026-03-31

### 수정
- 초대코드로 가족 참여 시 빈 가족 삭제 실패 수정 (FK 제약 위반)

## v1.4.0 — 2026-03-31

### 추가
- **PWA 설치 지원** — 홈화면에 앱 아이콘 추가 가능 (manifest, 아이콘, Apple 메타태그)
- Service Worker에 fetch 핸들러 추가 (Chrome PWA 설치 조건 충족)
- VAPID 키 설정으로 푸시 알림 활성화

### 개선
- 전체 프로젝트 "유통기한" → "소비기한" 용어 통일 (프론트엔드, 백엔드, 문서)
- 회원가입 페이지 문구 간소화
- 설정 페이지 푸시 알림 꺼짐 상태에서 불필요한 설명 문구 제거

## v1.3.0 — 2026-03-31

### 추가
- **푸시 알림** — Web Push API로 소비기한 알림을 기기에 직접 전송. 설정 페이지에서 ON/OFF 가능
- **알림 스케줄러** — APScheduler가 15분 간격으로 만료 체크 + 푸시 전송 (설정한 알림 시간에 맞춰 발송)
- 알림 설정이 가족별로 분리됨 (이전: 전체 공유 → 이제: 가족마다 독립 설정)
- 설정 페이지에 **푸시 시간 선택기** 추가 (30분 단위)
- 설정 페이지에 **알림 주기 토글** 컴포넌트 신규 구현
- 식재료 목록 API에 페이지네이션 지원 (`limit`, `offset` 파라미터, 기존 응답 하위 호환)
- 알림 로그 API 페이지네이션 (`limit=20`, `offset`)
- 알림 페이지에 "더 보기" 버튼
- Service Worker 등록 (푸시 수신 전용, 캐싱 없음)
- iOS Safari에서 PWA 설치 안내 메시지 표시
- `.env.example`에 VAPID 키 항목 + 생성 방법 추가

### 성능
- DB 커넥션 풀 설정 (`pool_size=10`, `max_overflow=20`, `pool_pre_ping`)
- 식재료/알림 로그 테이블에 복합 인덱스 추가
- 만료 체커를 벌크 쿼리로 최적화 (N*M 개별 쿼리 → 가족당 1회 벌크)
- 만료 중복 체크를 LIKE 패턴 → `days_before` 정확 매칭으로 변경

### 보안
- 알림 설정 IDOR 수정 — 다른 가족의 설정을 수정할 수 없도록 `family_id` 소유권 검증 추가
- VAPID 키 미설정 시 서버가 503 반환 (앱 시작은 허용, 경고 로그)

### 수정
- 설정 페이지 버전 v1.2.0 → v1.3.0 업데이트

## v1.2.0 — 2026-03-30

### 보안
- SECRET_KEY 기본값 사용 시 경고 추가
- 가족 IDOR 수정 (family_id 소유권 검증)
- 알림 설정 엔드포인트에 인증 추가
- Gemini API rate limiting 추가 (slowapi, 환경변수로 조절 가능)
- 비밀번호 최소 8자 정책 적용
- 식품안전나라 API HTTP→HTTPS 변경

### 추가
- 가족 마스터를 master_id 필드로 명시적 관리
- 식재료별 추이 탭에서 검색어 없을 때 전체 식재료 목록 표시
- 인벤토리에서 식재료 클릭 시 수정 모달 (이름, 가격, 매장, 소비기한)
- 직접등록 시 Gemini normalizer 사용 (브랜드명 정확한 제거)
- 회원가입 비밀번호 검증 에러 한국어 메시지 표시
- Gemini 모델 fallback 체인 (2.5-flash → 2.0-flash 자동 전환)
- 스캔 503 에러 시 자동 재시도 (최대 2회, exponential backoff)

### 수정
- 로그인 사용자에 따라 마스터가 달라지던 문제 해결
- 공동 편집 설정을 마스터만 변경 가능하도록 제한
- 가격 검색 시 부분 매칭으로 엉뚱한 결과가 나오던 문제 해결
- 매장별 비교가 가격 이력과 불일치하던 문제 해결
- 프론트엔드 비밀번호 minLength 6→8 백엔드와 일치
- Safari 호환성 (개발환경 쿠키 secure=false)
- 스캔 에러 시 Gemini 내부 에러 대신 한국어 메시지 표시
- 식재료 수정 시 이름 변경되면 normalized_name 재생성
- 3자 이하 이름은 Gemini 호출 스킵 (API 호출 절약)
- 마스터 탈퇴/이동 시 남은 멤버 중 가장 오래된 사람에게 마스터 자동 이전

## v1.1.1 — 2026-03-30

### 디자인 개선
- 로그인 폼 좌측 컬러 바 제거 (AI 생성 느낌 제거)
- 데스크톱 레이아웃 너비 최적화 (max-width 1024px)
- 제목 크기 상향 — 냉장고 화면 H1 24px, 섹션 제목 16px
- 하단 네비게이션 터치 타겟 44px 이상 보장
- 빈 상태 이모지(🥬)를 Material Icon으로 교체
- hardcoded 색상 값을 디자인 토큰 기반 `color-mix()`로 전환
- `1px solid` 구분선을 배경색 전환으로 교체 (No-Line 룰 준수)
- 저작권 연도 2024 → 2026 수정

## v1.1.0 — 2026-03-30

### 새로운 기능
- **Gemini 영수증 스캔** — 영수증 촬영 한 번에 식재료/가격/매장명 자동 인식. 세제, 생활용품 자동 제외
- **AI 레시피 추천** — 식품안전나라 API + Gemini AI 보충. 냉장고 재료 기반 맞춤 추천
- **가계부** — 월별 지출 바 차트, 식재료별 가격 추이, 매장별 비교, 인플레이션 알림
- **나만의 레시피** — 직접 레시피 등록/수정/삭제, 사진 업로드, 냉장고 재료 매칭 시 추천에 포함
- **가족 구성원 내보내기** — 마스터가 구성원을 내보내기 가능
- **월별 예산 설정** — 예산 초과 시 경고, 냉장고 화면에 프로그레스 바

### 개선
- 직접 등록에 가격/매장명/수량(자유 텍스트) 필드 추가
- 레시피 출처 표시 (나의 레시피 / 식품안전나라 / AI 추천)
- 식재료 이름 정규화 (베지밀B → 두유, 냉동 삼겹살과 생삼겹살 구분)
- 가계부 식재료 검색 자동완성 (구매 이력 기반)

### 버그 수정
- 직접 등록 실패 (quantity 타입 불일치)
- 모달이 네비/헤더에 가려지는 문제
- 레시피 매칭 루프 break 버그
- 등록 시 Gemini 동기 호출로 인한 지연
- 스캔 결과 모달 스크롤 안 되는 문제

### 디자인
- 터치 타겟 44px 최소 보장
- No-Line 규칙 적용 (구분선 제거)
- 섹션 제목 크기 개선
- 스캔 페이지 카메라 영역 축소

## v1.0.0 — 2026-03-22

### 최초 릴리스
- 식재료 등록/관리 (냉장/냉동/실온 필터링)
- 소비기한 D-Day 추적 + 알림
- 가족 공유 (초대 코드, 공동 편집)
- 보관기한 자동 추천 (221종 USDA FoodKeeper DB)
- 실시간 자동완성 검색
