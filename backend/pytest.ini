[pytest]
# conftest.py 가 @pytest_asyncio.fixture 를 쓰고 테스트는 @pytest.mark.asyncio 를 쓴다.
# asyncio_mode 를 고정해두지 않으면 pytest-asyncio 버전에 따라 스위트가 통째로
# 수집만 되고 실행되지 않는다.
asyncio_mode = auto
testpaths = tests
